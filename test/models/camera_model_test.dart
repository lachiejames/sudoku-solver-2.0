import 'package:flutter_test/flutter_test.dart';

void main() {
  group('CameraState ->', () {
    // TODO: write a test for the cameraState that doesn't fail
    // availableCameras -> MissingPluginException(No implementation found for method availableCameras on channel plugins.flutter.io/camera)
    test('initialised with correct values', () async {});
  });
}
